export const metadata = {
  title: "Egypt Explorer | Sign Up",
  description: "Join our Egypt explorer family now.",
};

export default function SignupLayout({ children }) {
  return (
    <section className="mx-auto px-6 flex items-center justify-center">
      {children}
    </section>
  );
}
